# 일반/증강 요트 다이스 작업 계획 및 진행 기록

> **문서 종류**: 작업 지시서 (AI 대상)
> **코드 대조 기준**: 2026-09-13 · 커밋 `8cc0273`
> 이 문서는 위 시점의 코드에서 확인된 것만 기술합니다. 계획 항목은 상태 표기로 구분합니다.
> 작성 원칙은 [`docs/README.md`](../README.md)를 보십시오.

## 1. 문서 목적

이 문서는 `Tessera`의 일반 요트 다이스와 증강 요트 다이스를 구현하기 위한 기준 계획서다.

작업이 중단되거나 담당자가 바뀌더라도 다음 내용을 확인해 바로 이어서 작업할 수 있어야 한다.

- 현재 구현 상태
- 지금 진행 중인 작업
- 바로 다음에 해야 할 구체적인 행동
- 단계별 범위와 완료 조건
- 이미 내린 설계 결정
- 테스트 결과와 알려진 문제
- 이전 작업 세션의 변경 내용

작업을 시작하거나 종료할 때는 반드시 이 문서의 `2. 현재 진행 포인터`와 `13. 작업 세션 로그`를 갱신한다.

---

## 2. 현재 진행 포인터

| 항목 | 현재 값 |
|---|---|
| 전체 상태 | M1~M6 완료, M7 완료 (T1~T4; T5~T12는 `M17`로 이동 `D-038`), M8 증강 시스템 구조 리팩토링 완료, M9 프리팹 이관 완료, M10 완료, M11 컨트롤러 분해 완료, M12·M13 구현 완료(`M13-T4`는 `M16-T6`으로 이관), M14·M15 완료, M16 셀 셰이딩 채택 보류(`DEFERRED`, 인프라 유지, `D-037`) |
| 현재 마일스톤 | `M17` 증강 요트 로컬 핫시트 완성 |
| 현재 작업 | `M17-T22` 깃펜 외부 모델 교체. 코드·에셋 작업 완료, 화면 확인 남음. `M17-T9-1-5`·`M17-T18-5` 화면 확인도 아직 열려 있다 |
| 다음 행동 | 화면 확인 세 건을 한 세션에서 처리. (1) `M17-T22` 깃펜이 잉크통 꽂힘 자세와 필기 자세에서 제대로 보이는지, 그리고 족보 글자를 가리는 범위. (2) `M17-T18-5` 호버 필기 연출. (3) `M17-T9-1-5` 연기가 킵 안 된 주사위를 가리는 0.25초 동안 눈 표시와 후보 점수 갱신이 보류되는지 |
| 마지막 완료 작업 | 56 `dice-alchemy` 연기 스프라이트·표시 갱신 지연 큐·`AugmentVfxPlanner` 확장·연기 컴포넌트 구현 (`M17-T9-1-1`~`M17-T9-1-4`) |
| 차단 요소 | 없음 |
| 마지막 갱신일 | 2026-09-13 |

### 상태 표기 규칙

| 상태 | 의미 |
|---|---|
| `TODO` | 아직 시작하지 않음 |
| `DOING` | 현재 작업 중 |
| `DONE` | 구현과 검증이 완료됨 |
| `BLOCKED` | 외부 결정이나 선행 작업 때문에 진행할 수 없음 |
| `DEFERRED` | 현재 범위에서 의도적으로 연기함 |
| `OUT` | 이번 프로젝트 범위에서 제외함 |
| `DROPPED` | 전제가 바뀌어 작업 자체를 폐기함. 사유와 날짜를 함께 남긴다 |
| `VOID` | 대상이 사라져 태스크가 무효가 됨. 행을 지우지 않고 사유와 날짜를 남긴다 |

한 번에 하나의 작업만 `DOING`으로 둔다. 작업을 마치면 `DONE`으로 바꾸되 다음 작업은 자동으로 시작하지 않는다. 사용자가 다음 작업 ID의 시작 또는 진행을 명시적으로 언급한 경우에만 해당 작업을 `DOING`으로 지정한다.

---

## 3. 확정된 방향과 범위

### 3.1 구현 순서

```text
이전 증강 시스템 조사
    ↓
권위와 무관한 순수 C# 게임 코어
    ↓
일반 요트 로컬 핫시트 완성
    ↓
일반/증강 게임 모드 분리
    ↓
대표 증강 수직 구현
    ↓
증강 55개 전체 이식
    ↓
증강 시스템 그래픽 요소 완성
    ↓
증강 요트 로컬 핫시트 완성
    ↓
네트워크 지연·중복·재동기화 검증
    ↓
EOS 또는 Steam 기반 호스트 온라인 연결
```

### 3.2 현재 확정 사항

1. 일반 요트가 기본 규칙이며 증강 요트는 기본 규칙에 증강 규칙을 합성한다.
2. 외부 권위 서버보다 로컬 핫시트 구현을 먼저 완료한다.
3. 레이팅, MMR, 순위표, 승패 기반 서버 보상은 현재 범위에서 제외한다.
4. 온라인 초기 버전은 별도 권위 서버 없이 플레이어 호스트 권위를 사용할 수 있게 준비한다.
5. 게임 규칙은 `UnityEngine`, `MonoBehaviour`, 씬 오브젝트에 의존하지 않는 순수 C#으로 작성한다.
6. Unity 클라이언트는 명령을 보내고 결과를 연출하는 역할을 맡는다.
7. 주사위 값과 베이킹 프리셋 선택 결과는 하나의 명령 결과로 함께 확정한다.
8. 현재는 일반/증강 모드가 하나의 공유 씬을 사용하는 구조를 우선한다.
9. 두 모드의 아트와 배치가 크게 달라질 때만 공통 프리팹을 기반으로 얇은 별도 씬을 만든다.
10. 이전 JavaScript 코드를 줄 단위로 번역하지 않고, 동작과 테스트를 기준으로 C#에 재설계한다.

### 3.3 현재 제외하거나 연기한 기능

| 기능 | 상태 | 다시 검토하는 시점 |
|---|---|---|
| 레이팅/MMR | `OUT` | 경쟁 모드 기획 확정 시 |
| 신뢰 가능한 원격 권위 서버 | `DEFERRED` | 레이팅·재화 보상 도입 시 |
| 서버 전용 Unity 인스턴스 | `OUT` | 실시간 고빈도 게임 규칙이 생길 때 |
| 일반/증강 씬 완전 복제 | `DEFERRED` | 두 모드의 시각 구성이 실제로 달라질 때 |
| 호스트 마이그레이션 | `DEFERRED` | 온라인 기본 매치가 안정화된 뒤 |
| `M17-T13`~`M17-T15` 그래픽 폴리시 (사운드·촉각, 그래픽 폴백, 해상도·성능 검증; 구 `M7-T10`·`T11`·`T12`) | `DEFERRED` | `M17` 핵심 완주 뒤 (`D-038`) |
| M16 셀 셰이딩 (재료 단계 셀 램프, Baseline/Cel A/B) | `DEFERRED` | 그래픽 패스 재개 시. 완료 조건 미충족·전제 오류로 채택 보류, 인프라는 유지 (`D-037`) |

---

## 4. 현재 프로젝트 기준선

### 4.1 Tessera에서 확인된 상태

| 영역 | 상태 | 근거/비고 |
|---|---|---|
| 기본 2인 요트 점수 계산 | 구현됨 | `Assets/Scripts/Games/AugmentedYacht/YachtGameRules.cs` |
| 12라운드 턴 진행 | 구현됨 | `YachtGameSession` |
| 기본 규칙 EditMode 테스트 | 일부 구현됨 | `Assets/Editor/YachtGameRulesTests.cs` |
| 베이킹 프리셋 로딩/재생 | 구현됨 | `DicePresetCatalog`, `BakedDiceController` |
| 주사위 값 결정 | 컨트롤러에 결합됨 | `AugmentedYachtController`가 `UnityEngine.Random` 사용 |
| 베이킹 프리셋 선택 | 컨트롤러에 결합됨 | 일반 5주사위 프리셋을 직접 선택 |
| 주사위 킵 상태 | 프레젠테이션 계층에 일부 존재 | 권위 상태로 이동 필요 |
| 증강 정의 데이터 | 55개 존재 | `Assets/StreamingAssets/WebSource/data/augments.json` |
| 증강 카드 트레이 비주얼 | 구현됨 | `Assets/Scripts/Tabletop/AugmentCardTray.cs` |
| 실제 증강 런타임 | 미구현 | 트리거, 상태, 효과 처리기 필요 |
| 네트워크 모듈 | 비어 있음 | `Assets/Scripts/Network` |
| 일반/증강 모드 전환 | 미구현 | 공유 씬 모드 분기 필요 |

### 4.2 현재 구조에서 주의할 점

- `AugmentedYachtController.cs`가 게임 진행, 주사위 난수, 연출, 월드 생성, 입력을 폭넓게 담당한다.
- 일반 모드 씬에서 `AugmentCardTray` 오브젝트만 삭제해도 컨트롤러가 런타임에 다시 생성한다.
- `YachtGameSession`은 점수와 턴을 관리하지만 주사위 값·종류·킵 상태를 완전히 소유하지 않는다.
- 온라인 이전 가능성을 확보하려면 주사위와 킵 상태도 직렬화 가능한 게임 상태로 이동해야 한다.
- 증강 이식은 큰 컨트롤러에 조건문을 추가하는 방식으로 진행하지 않는다.
- 기존 사용자 변경과 무관한 코드 및 아트는 수정하지 않는다.

### 4.3 이전 프로젝트 레퍼런스

로컬 경로:

```text
C:\Users\dpcks\OneDrive\문서\yec\augmented-dice
```

GitHub:

```text
https://github.com/dpcks9677/augmented-dice
```

핵심 파일:

| 파일 | 참고할 내용 |
|---|---|
| `src/augments.json` | 55개 증강 표시 데이터 |
| `src/scoreEngine.js` | 증강 적용 점수 계산 |
| `src/augmentRules.js` | 충돌, 획득, 진행도 계산 |
| `src/authoritativeGame.js` | 상태, 턴, 굴림, 드래프트, 증강 행동 |
| `src/game/augments/augmentDraft.js` | 증강 선택지 생성 |
| `src/localDiceResultProvider.js` | 로컬 주사위 결과와 프리셋 키 선택 |
| `src/dicePresetRules.js` | 주사위 구성별 프리셋 파일 결정 |
| `src/server/DiceServer.js` | 명령 검증, 결과 및 애니메이션 브로드캐스트 |
| `tests/augmentRules.test.mjs` | 증강 규칙 기준 테스트 |
| `tests/authoritativeGame.test.mjs` | 전체 권위 상태 기준 테스트 |

이전 구현은 증강 로직이 `scoreEngine.js`, `augmentRules.js`, `authoritativeGame.js`에 분산되고 일부 증강 ID가 조건문에 직접 들어가 있다. 동작 기준으로는 활용하되 구조를 그대로 복제하지 않는다.

---

## 5. 목표 아키텍처

### 5.1 모듈 관계

```text
Tessera.Games.Yacht
├─ 기본 게임 상태
├─ 기본 점수 계산
├─ 명령과 결과
├─ 일반 요트 규칙
└─ 로컬 권위 구현

Tessera.Games.AugmentedYacht
├─ 증강 정의
├─ 증강 런타임 상태
├─ 드래프트 규칙
├─ 증강 효과 처리
└─ 기본 요트 규칙과 합성

Tessera.Dice
├─ 베이킹 프리셋 카탈로그
├─ 주사위 프레젠테이션
└─ 결과 재생

Tessera.Tabletop
├─ 공통 테이블 오브젝트
└─ 증강 전용 테이블 오브젝트

Tessera.Network
├─ 명령 전송
├─ 상태 스냅샷
├─ 호스트 권위 어댑터
└─ EOS/Steam 어댑터
```

위 구조는 책임 경계다. 실제 폴더 배치는 `D-028`에서 확정했다: 증강 관련 코드는 전부 `Assets/Scripts/Games/AugmentedYacht/` 아래에 두되, 순수 C# 로직은 `Logic/`, MonoBehaviour 프레젠테이션은 `Presentation/`으로 나눈다. 네임스페이스는 `Tessera.Games.Yacht`를 유지한다(`D-021`). asmdef 분리는 아직 하지 않는다.

### 5.2 핵심 처리 흐름

```text
UI 입력
  ↓
GameCommand
  ↓
IGameAuthority.ExecuteAsync
  ↓
게임 상태 및 규칙 검증
  ↓
GameCommandResult
  ├─ 새 상태 스냅샷
  ├─ 게임 이벤트
  ├─ 최종 주사위 값
  └─ 베이킹 프리셋 정보
  ↓
Unity 프레젠테이션
```

### 5.3 핵심 데이터 초안

```text
YachtGameState
├─ Mode
├─ Phase
├─ CurrentPlayerIndex
├─ CurrentRound
├─ RollsRemaining
├─ Dice[]
├─ Players[]
├─ DraftState
├─ AugmentStates[]
└─ Revision

YachtDieState
├─ Id
├─ Type
├─ Value
├─ IsKept
└─ PromotionLevel

GameCommand
├─ CommandId
├─ ExpectedRevision
├─ PlayerId/PlayerIndex
├─ Type
└─ Payload

GameCommandResult
├─ Accepted
├─ ErrorCode
├─ State
├─ Events[]
└─ RollPresentation

RollPresentation
├─ PresetFile
├─ PresetIndex
├─ IsMirrored
├─ FinalValues[]
└─ Duration
```

이 이름은 구현 과정에서 조정할 수 있지만 책임을 다시 프레젠테이션 계층으로 되돌리면 안 된다.

### 5.4 증강 처리 원칙

- JSON에는 ID, 이름, 설명, 대상, 표시 정보 같은 정적 데이터만 둔다.
- 실제 규칙은 컴파일되고 테스트되는 C# 처리기에 둔다.
- 범용 이벤트 버스를 과도하게 만들지 않고 고정된 처리 지점을 사용한다.
- 예상 처리 지점은 다음과 같다.

```text
OnAugmentSelected
OnTurnStarted
BeforeRoll
AfterRoll
BeforeScorePreview
BeforeScoreCommit
AfterScoreCommit
OnTurnEnded
OnGameEnded
ManualAction
```

- 증강 효과 처리 순서가 결과에 영향을 준다면 우선순위를 명시하고 테스트한다.
- 한 증강의 진행 상태는 다른 증강의 임의 딕셔너리에 섞지 않는다.

---

## 6. 마일스톤 요약

| ID | 마일스톤 | 상태 | 선행 작업 | 핵심 결과물 |
|---|---|---|---|---|
| `M0` | 작업 기준선과 계획서 | `DONE` | 없음 | 이 문서 |
| `M1` | 기존 증강 조사 및 이식 명세 | `DONE` | `M0` | 55개 증강 이식표, 테스트 기준선 |
| `M2` | 권위 독립형 기본 게임 코어 | `DONE` | `M1` | 상태·명령·결과·난수·로컬 권위 |
| `M3` | 일반 요트 로컬 핫시트 | `DONE` | `M2` | 일반 게임 전체 완주 가능 |
| `M4` | 일반/증강 모드 분리 | `DONE` | `M3` | 공유 씬 모드 전환 |
| `M5` | 대표 증강 수직 구현 | `DONE` | `M4` | 서로 다른 유형의 증강 5종 |
| `M6` | 증강 55개 전체 이식 및 명시적 제외 처리 | `DONE` | `M5` | 45개 구현·6개 삭제·4개 미구현 확정과 회귀 테스트 |
| `M7` | 증강 시스템 그래픽 요소 추가 | `DONE` | `M6` | 증강 카드 공통 비주얼·스크롤 4종·상태 표현·트레이 배치 (T1~T4). T5~T12(특수 주사위·VFX·상태 표시·폴백·검증)는 `M17`로 이동 (`D-038`) |
| `M8` | 증강 시스템 구조 리팩토링 | `DONE` | `M6` | 발동 시점 인터페이스, 증강별 핸들러·상태 분리, 증강별 회귀 테스트 |
| `M9` | 테이블 오브젝트 프리팹 이관 | `DONE` | `M8` | 프롭 프리팹화, 씬 저작 전환, 코딩 규약·ADR 문서 |
| `M10` | 족보 표·증강 카드 UI 월드 스페이스 전환 | `DONE` | `M9` | 계층 기반 배치, 픽셀 필터 우회 전용 카메라 |
| `M11` | 프레젠테이션 계층 리팩토링 | `DONE` | `M9` | 컨트롤러 분해, 프레젠테이션 상태 통합 |
| `M12` | 픽셀 필터 엣지 검출 도입 | `DONE` | `M11` | 뎁스 외곽선·노멀 하이라이트, 기존 필터와 A/B 전환 |
| `M13` | 픽셀 필터 색 양자화 | `DONE` | `M12` | sRGB 포스터라이즈·아트 가이드 팔레트·Bayer 디더, 3단 모드 전환 |
| `M14` | 트레이 확대와 보드 좌표 상수 단일화 | `DONE` | `M13` | 트레이 배율 0.06, 좌표 상수가 `DiceBoardMetrics` 한 곳에서만 정의됨 |
| `M15` | Unity 물리 기반 주사위 프리셋 베이커 | `DONE` | `M14` | 프리셋을 Unity PhysX로 굽고 분포·인식도·판정 시간으로 평가 |
| `M16` | 셀 셰이딩 전환과 픽셀 격자 정합 | `DEFERRED` | `M14` | Baseline/Cel A/B·셀 셰이더·측정 도구 인프라 완료. 완료 조건 미충족·전제 오류로 채택 보류, 기본값 Baseline 유지 (`D-037`) |
| `M17` | 증강 요트 로컬 핫시트 완성 | `TODO` | `M11` | 전체 증강 게임 완주 + 완주에 필요한 그래픽(특수 주사위·VFX·상태 표시). 구 `M7-T5~T12` 흡수 (`D-038`) |
| `M18` | 네트워크 준비 검증 | `TODO` | `M17` | 지연·중복·재동기화 대응 |
| `M19` | EOS/Steam 호스트 온라인 | `TODO` | `M18` | 비레이팅 온라인 매치 |
| `M20` | 원격 권위/레이팅 | `DEFERRED` | 별도 기획 | 필요할 때만 진행 |

마일스톤 번호는 두 번 재조정했다.

- `D-029`(2026-09-04): 기존 `M7.5`→`M8`, 기존 `M8`→`M11`, `M9`→`M12`, `M10`→`M13`, `M11`→`M14`로 밀렸고 `M9`·`M10`이 신설됐다.
- `D-035`(2026-09-06): 소수점 마일스톤을 없애고 전부 정수로 다시 매겼다. `M9.5`→`M10`, `M10`→`M11`, `M10.5`→`M12`, `M10.6`→`M13`, `M10.7`→`M14`, `M10.9`→`M15`, `M10.8`→`M16`, `M11`→`M17`, `M12`→`M18`, `M13`→`M19`, `M14`→`M20`. 하위 작업 ID도 함께 이동했다(`M10.6-T4`→`M13-T4` 등).

§11 결정 기록과 §13 작업 세션 로그의 과거 항목은 기록 보존을 위해 원래 ID를 그대로 둔다. 코드 주석의 마일스톤 표기도 커밋 시점을 가리키는 역사 기록이므로 바꾸지 않는다.

---

## 7. 상세 작업 목록

> **완료된 마일스톤(`M0` ~ `M16`)의 태스크표와 실행 기록은 [`milestones/completed.md`](milestones/completed.md)로 분리했습니다.**
> 아래에는 진행 중인 `M17`과 미착수 `M18` ~ `M20`만 둡니다. 전체 진척은 §6 마일스톤 요약표를 보십시오.

## M17. 증강 요트 로컬 핫시트 완성

| ID | 작업 | 상태 | 완료 조건 |
|---|---|---|---|
| `M17-T1` | 플레이어 교대 가림 화면 | `TODO` | 상대의 비공개 선택지를 바로 볼 수 없음 |
| `M17-T2` | 플레이어별 드래프트 UI | `TODO` | 현재 플레이어만 선택 가능함 |
| `M17-T3` | 보유 증강 표시 | `TODO` | 대상 족보와 진행 상태를 확인 가능함 |
| `M17-T4` | 증강 수동 행동 UI | `TODO` | 사용 가능 조건과 남은 횟수가 표시됨 |
| `M17-T5` | 시간 관련 증강과 타이머 | `TODO` | 로컬 규칙 기준으로 일관되게 처리됨 |
| `M17-T6` | 게임 종료 요약 | `TODO` | 점수와 증강 결과를 확인 가능함 |
| `M17-T7` | 반복/무작위 전체 게임 테스트 | `TODO` | 여러 증강 조합으로 완주함 |
| `M17-T8` | 특수 주사위 외형 구현 (구 `M7-T5`) | `DONE` | 8종 전부 화면에서 구분 확인(2026-09-07). 8면체 면의 숫자는 `CrispUI` 레이어로 올려 픽셀 필터를 우회 |
| `M17-T9` | 증강 발동 VFX와 전환 연출 (구 `M7-T6`) | `TODO` | 발동 주체·대상·효과 결과가 과도한 텍스트 없이 전달됨. 사양과 하위 구현 계획은 [`docs/agent/m17_vfx_spec.md`](m17_vfx_spec.md) |
| `M17-T9-1` | 56 `dice-alchemy` 연기 가림 연출 | `DOING` | 연기가 킵 안 된 주사위를 가린 사이에 눈이 바뀌고, 점수표 후보 점수가 먼저 결과를 알리지 않음. `M17-T9-1-1`~`M17-T9-1-4` 완료, `M17-T9-1-5` Play 모드 화면 확인 남음(2026-09-09). 상세는 VFX 사양서 §9 |
| `M17-T10` | 점수표 증강 표시 (구 `M7-T7`) | `TODO` | 교체된 족보·보너스·잠금·진행 조건을 점수표에서 확인 가능. `M17-T3`과 함께 |
| `M17-T11` | 진행형 증강 상태 표시 (구 `M7-T8`) | `TODO` | 횟수·남은 턴·누적치·성공/실패 확인. `M17-T3`과 함께 |
| `M17-T12` | 수동 증강 행동 피드백 (구 `M7-T9`) | `TODO` | 사용 가능 여부·입력 확인·성공/실패 이유 표현. `M17-T4`와 함께 |
| `M17-T13` | 증강 사운드·촉각 피드백 (구 `M7-T10`) | `DEFERRED` | 후속 오디오·플랫폼 작업으로 이관 유지 |
| `M17-T14` | 그래픽 폴백과 누락 검증 (구 `M7-T11`) | `DEFERRED` | `M17` 핵심 완주 뒤. 원본·픽셀 보정본 누락 시 안전 표시 |
| `M17-T15` | 해상도·가독성·성능 검증 (구 `M7-T12`) | `DEFERRED` | `M17` 핵심 완주 뒤. 32·48·64px·목표 해상도, 프레임·메모리 |
| `M17-T17` | 드래프트 제시 구성과 선공 순서 규칙 | `DONE` | 같은 대상 족보를 교체하는 변형 증강이 한 제시에 둘 이상 나오지 않고, 1번 증강은 무작위·2·3번 증강은 총점이 낮은 쪽이 먼저 선택(2026-09-08). `D-039` |
| `M17-T16` | 테이블 나뭇결 픽셀화 | `DONE` | 결 경계가 계단으로 끊기고, 판자 4장이 서로 다른 결·옹이·균열을 가짐(2026-09-08). `D-037`이 남긴 "나무결 텍스처 표면" 항목 해소 |
| `M17-T18` | 점수 칸 호버 깃펜 필기 연출 | `DOING` | 기입 가능한 칸을 가리키면 깃펜이 잉크통에서 나와 그 칸에 닙을 대고, 칸을 옮기면 따라붙고, 포인터를 빼면 잉크통으로 복귀. 그동안에도 점수표 전체가 읽힘. `M17-T18-1`~`M17-T18-4` 완료, `M17-T18-5` 화면 확인 남음(2026-09-09). 상세는 [`docs/agent/m17_quill_hover_animation.md`](m17_quill_hover_animation.md) |
| `M17-T19` | 깃펜 깃털 흰색 전환과 갈라짐 연출 | `DROPPED` | 절차적 깃펜 자체를 폐기하고 외부 로우폴리 모델 `Assets/Art/Reference/quill_pen_low.fbx`로 교체(2026-09-12). 화면이 480x270 격자로 필터링돼 여기서 맞춘 디테일이 남지 않는데도 굽고 눈으로 확인하는 왕복이 계속 필요해 비용이 맞지 않았다. 계획서는 [`docs/archive/plans/quill_feather_appearance_plan.md`](../archive/plans/quill_feather_appearance_plan.md)에 폐기 표시와 함께 남김 |
| `M17-T22` | 깃펜 외부 모델 교체 | `DOING` | 절차적 깃펜(닙·칼라·깃대·깃판) 8개를 FBX 인스턴스 하나로 교체. `InkwellAndQuill`은 잉크통만 만들고 깃펜은 직렬화된 모델 참조를 붙인다. 텍스처는 256 알베도 한 장(BaseColor+Opacity 합성). 코드·에셋 작업 완료, 화면 확인 남음(2026-09-12) |

`M17-T8`~`M17-T12`의 시각 사양은 [`docs/agent/m7_graphics_spec.md`](m7_graphics_spec.md)를 기준으로 한다 (구 `M7-T5`~`M7-T9`). `M17-T8` 특수 주사위는 `augmented-dice` 프로젝트 구현을 재사용한다.

M17 완료 조건:

- 로컬 핫시트만으로 일반/증강 게임을 모두 선택해 완주할 수 있다.
- 씬 재시작 없이 새 게임을 시작할 수 있다.
- 잘못된 입력에서 상태가 손상되지 않는다.
- `M17-T8`~`M17-T12`: 특수 주사위 논리·외형 일치, 증강 발동 주체·대상·결과 전달, 점수표·진행형·수동 증강 상태를 화면에서 확인 가능하다.
- `M17-T13`~`M17-T15`(사운드·폴백·해상도/성능)는 `DEFERRED`이며 핵심 완주 완료 조건에서 제외한다 (`D-038`).

## M18. 네트워크 준비 검증

| ID | 작업 | 상태 | 완료 조건 |
|---|---|---|---|
| `M18-T1` | 명령/결과/상태 직렬화 테스트 | `TODO` | 왕복 직렬화 후 상태가 동일함 |
| `M18-T2` | `CommandId` 중복 방지 | `TODO` | 동일 명령이 한 번만 적용됨 |
| `M18-T3` | `ExpectedRevision` 충돌 처리 | `TODO` | 오래된 명령이 명확한 오류로 거부됨 |
| `M18-T4` | 지연 시뮬레이터 | `TODO` | 200~500ms 지연에서도 UI가 안정적임 |
| `M18-T5` | 순서 뒤바뀜/재전송 테스트 | `TODO` | 잘못된 순서의 결과가 상태를 되돌리지 않음 |
| `M18-T6` | 전체 스냅샷 재동기화 | `TODO` | 임의 시점에서 화면을 복구할 수 있음 |
| `M18-T7` | `HostGameAuthority` 루프백 구현 | `TODO` | 실제 플랫폼 없이 호스트/게스트 흐름 검증 |
| `M18-T8` | `AugmentStateStore` 직렬화 | `TODO` | `{id, typeTag, payload}` 평탄화로 증강 상태가 왕복 직렬화됨 |

M18 완료 조건:

- 네트워크 전송 계층을 바꿔도 게임 규칙 코드를 수정하지 않는다.
- 연결이 잠시 끊긴 뒤 전체 상태로 복구할 수 있다.
- 연출 중 재동기화해도 최종 논리 상태가 우선한다.

## M19. EOS/Steam 호스트 온라인

플랫폼은 구현 시작 전에 다시 결정한다. EOS와 Steam을 동시에 결합하지 않고 하나의 최소 수직 흐름부터 완성한다.

| ID | 작업 | 상태 | 완료 조건 |
|---|---|---|---|
| `M19-T1` | 인증/로비 플랫폼 결정 기록 | `TODO` | EOS/Steam 선택 이유와 범위가 기록됨 |
| `M19-T2` | 로그인과 플레이어 ID 매핑 | `TODO` | 재접속 시 동일 플레이어를 식별함 |
| `M19-T3` | 친구 초대 또는 로비 참가 | `TODO` | 2인이 같은 매치에 참가함 |
| `M19-T4` | 호스트 선택 | `TODO` | 양쪽이 동일한 호스트를 인식함 |
| `M19-T5` | 명령 전송과 결과 브로드캐스트 | `TODO` | 게스트 명령이 호스트에서 검증됨 |
| `M19-T6` | 전체 상태 동기화 | `TODO` | 늦게 도착하거나 재접속한 클라이언트 복구 |
| `M19-T7` | 호스트 종료 정책 | `TODO` | 매치 종료 또는 마이그레이션 정책이 동작함 |
| `M19-T8` | 온라인 전체 게임 검증 | `TODO` | 일반/증강 모드 온라인 완주 |

M19 완료 조건:

- 외부 레이팅 서버 없이 친구 간 온라인 게임을 완주할 수 있다.
- 호스트가 주사위 결과와 프리셋을 결정하고 게스트가 동일하게 재생한다.
- 호스트 이탈과 연결 오류가 무한 대기 상태를 만들지 않는다.

## M20. 원격 권위 서버와 레이팅

현재는 `DEFERRED` 상태다. 다음 요구가 확정되면 별도 계획을 작성한다.

- MMR 또는 공식 랭크
- 승패 기반 재화/경험치
- 신뢰해야 하는 업적과 순위표
- 공개 경쟁 매칭
- 호스트 치팅 방지

도입 시 `RemoteGameAuthority`가 기존 `IGameAuthority`를 구현해야 하며 게임 규칙 자체는 다시 작성하지 않는다.

---

## 8. 공통 완료 기준

모든 구현 작업은 다음 기준을 만족해야 `DONE`으로 변경할 수 있다.

- 요청 범위와 관련된 코드만 수정했다.
- 새 규칙에는 정상·경계·거부 사례 테스트가 있다.
- 관련 EditMode 테스트가 통과한다.
- 플레이 모드에서 주요 흐름을 직접 확인했다.
- Unity Console에 새 컴파일 오류와 예외가 없다.
- 게임 코어에 `UnityEngine.Random`, `MonoBehaviour`, 씬 참조가 추가되지 않았다.
- UI와 애니메이션이 게임 규칙 상태를 임의로 변경하지 않는다.
- 결과 재생 후 화면과 상태 스냅샷의 값이 일치한다.
- 변경 파일과 검증 결과를 작업 세션 로그에 남겼다.
- 다음 작업 포인터를 갱신했다.

테스트를 실행하지 못했다면 `DONE`으로 표시하지 않고 이유와 필요한 검증을 로그에 기록한다.

---

## 9. 테스트 전략

### 9.1 이전 프로젝트 기준선

이전 프로젝트 루트에서 실행:

```powershell
npm test
npm run validate:augments
```

기존 동작을 수정하는 것이 아니라 이식 기준으로 사용한다. 기존 테스트가 실패한다면 실패를 먼저 기록하고, Unity 구현이 그 실패까지 모방하도록 만들지 않는다.

### 9.2 Unity EditMode 테스트

우선 검증 대상:

- 기본 점수 계산
- 전체 턴 진행
- 난수 주입과 결정성
- 명령 유효성 검증
- 주사위 킵과 재굴림
- 증강별 점수와 상태 변화
- 증강 조합 및 처리 순서
- 명령 중복과 revision 충돌
- 상태 직렬화 왕복

테스트 파일은 규칙 영역과 가까운 이름을 사용하되 기존 프로젝트 구조를 불필요하게 대규모 변경하지 않는다.

### 9.3 플레이 모드/수동 검증

- 일반 요트 한 게임 완주
- 증강 요트 한 게임 완주
- 모든 주사위 킵 후 굴림 거부
- 마지막 굴림 후 점수 기입
- 플레이어 교대 도중 입력 차단
- 특수 주사위별 프리셋 재생
- 턴 타이머 만료
- 게임 종료 후 재시작
- 상태 스냅샷 재적용

### 9.4 회귀 원칙

증강 기능을 추가할 때마다 일반 요트 테스트를 함께 실행한다. 일반 모드의 결과가 바뀌었다면 의도된 변경인지 결정 기록을 남기기 전에는 다음 작업으로 넘어가지 않는다.

---

## 10. 위험 요소와 대응

| ID | 위험 | 영향 | 대응 |
|---|---|---|---|
| `R-001` | 큰 컨트롤러에 규칙을 계속 추가 | 테스트와 온라인 이전이 어려워짐 | 상태와 명령부터 단계적으로 추출 |
| `R-002` | 씬을 완전 복제 | 두 씬의 조명·UI·오브젝트가 달라짐 | 공유 씬 우선, 필요 시 공통 프리팹 사용 |
| `R-003` | 이전 JS 구조를 그대로 번역 | 하드코딩과 상태 분산을 재현 | 동작 매트릭스와 테스트 기준 이식 |
| `R-004` | 주사위 값과 프리셋이 별도 난수 | 양쪽 클라이언트 연출 불일치 | 하나의 `RollPresentation` 결과로 확정 |
| `R-005` | 증강 처리 순서 불명확 | 조합에 따라 결과가 달라짐 | 고정 처리 지점·우선순위·조합 테스트 |
| `R-006` | 핫시트 드래프트 정보 노출 | 상대 선택지를 미리 확인 | 플레이어 교대 가림 화면 사용 |
| `R-007` | 네트워크부터 구현 | 규칙과 전송 오류를 동시에 디버깅 | 로컬 완성 후 지연 시뮬레이션 진행 |
| `R-008` | 호스트 권위를 신뢰 서버로 오해 | 호스트 치팅 가능 | 비레이팅 캐주얼 범위로 명시 |
| `R-009` | 특수 주사위 프리셋 누락 | 연출 실패 또는 잘못된 주사위 수 | 프리셋 호환성 자동 테스트 |
| `R-010` | 기존 사용자 변경과 충돌 | 작업 손실 가능 | 수정 전 `git status`, 정밀 수정, 관련 없는 파일 보존 |
| `R-011` | 증강 그래픽이 게임 규칙과 직접 결합 | 연출 중단이 상태 오류로 이어짐 | 그래픽은 확정된 이벤트와 상태를 읽기만 하도록 유지 |

---

## 11. 결정 기록 · 12. 미결정 사항

[`decisions.md`](decisions.md)로 분리했습니다. 결정 기록(`D-nnn`)과 미결정 사항(`Q-nnn`)이 그 문서에 있습니다.
새 결정을 추가할 때도 그 문서에 씁니다.

---

## 13. 작업 세션 로그

[`session_log.md`](session_log.md)로 분리했습니다. 최신 항목이 위에 옵니다.
새 세션 기록도 그 문서 최상단에 씁니다.


## 14. 작업 재개 절차

새 작업 세션은 다음 순서를 따른다.

1. 이 문서의 `2. 현재 진행 포인터`를 읽는다.
2. 가장 최근의 `13. 작업 세션 로그`를 읽는다.
3. `git status --short`로 기존 변경을 확인하고 사용자 변경을 보존한다.
4. 현재 작업 ID의 선행 작업과 완료 조건을 확인한다.
5. 관련 테스트를 먼저 실행해 시작 기준선을 확인한다.
6. 현재 작업이 `TODO`라면 사용자에게 시작 지시를 받기 전까지 구현하지 않는다. `DOING` 상태라면 해당 작업 범위만 구현한다.
7. 관련 테스트와 플레이 검증을 수행한다.
8. 상세 작업 상태를 `DONE` 또는 `BLOCKED`로 갱신한다.
9. 다음 작업을 자동으로 `DOING`으로 지정하지 않고 사용자 지시를 기다린다.
10. 진행 포인터와 세션 로그를 갱신한다.

### 중단 직전 최소 기록

시간이 부족해 작업을 완전히 마치지 못하더라도 다음 네 가지는 반드시 기록한다.

```text
현재 수정 중인 작업 ID
마지막으로 변경한 파일
마지막 테스트 결과
다음에 실행할 정확한 행동 한 줄
```

### 차단된 경우

- 막힌 원인을 재현 가능한 형태로 기록한다.
- 시도한 해결 방법을 기록한다.
- 사용자 결정이 필요하면 질문을 `미결정 사항`에 추가한다.
- 진행 가능한 다른 마일스톤으로 임의 이동하지 않는다.

---

## 15. 빠른 참조

### 현재 최우선 작업

```text
main 병합 준비 (M16 DEFERRED · D-037)
feature/yacht-dice-migration(M1~M15 + 구조) → main --no-ff 병합. 이후 M17(구 M7-T5~T12 흡수)은 새 브랜치
```

### 현재 메인 씬

```text
Assets/Scenes/Augmented Dice.unity
```

### 현재 기본 규칙

```text
Assets/Scripts/Games/AugmentedYacht/YachtGameRules.cs
```

### 현재 게임 컨트롤러

```text
Assets/Scripts/Games/AugmentedYacht/AugmentedYachtController.cs
```

### 현재 증강 데이터

```text
Assets/StreamingAssets/WebSource/data/augments.json
```

### 원칙 한 줄 요약

```text
일반 요트를 순수 C# 코어로 먼저 완성하고, 증강을 합성한 뒤, 마지막에 네트워크 전송 계층을 연결한다.
```
